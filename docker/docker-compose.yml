version: "3.8"
services:
  web:
    image: python:3.11-alpine
    command: python -m http.server 8080
    volumes:
      - ./dist:/app:ro
    working_dir: /app
    ports:
      - "8080:8080"
    mem_limit: 128m
    cpus: 0.25
